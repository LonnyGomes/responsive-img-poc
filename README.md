# Responsive Image POC

A proof of concept example of responsive images for use in a different project.

## Usage

This project uses parcel to serve and build the HTML bundle

```bash
npm install
npm start
```
